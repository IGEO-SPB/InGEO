CREATE TABLE CL_SOIL_GROUP_TYPE
(
    ID        int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    GT_NAME   VARCHAR,
    No_pp     int,
    STATUS    varchar,
    CREATOR   VARCHAR,
    DATE_CR   varchar,
    ARCHIVER  VARCHAR,
    DATE_ARCH varchar
);
