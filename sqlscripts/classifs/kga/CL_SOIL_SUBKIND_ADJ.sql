CREATE TABLE CL_SOIL_SUBKIND_ADJ
(
    ID        int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SSA_NAME  VARCHAR,
    STATUS    varchar,
    CREATOR   VARCHAR,
    DATE_CR   varchar,
    ARCHIVER  VARCHAR,
    DATE_ARCH varchar,
    SSA_DESCR VARCHAR
);